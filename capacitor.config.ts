import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'react-minesweeper',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
